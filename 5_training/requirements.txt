# ReconMind Training — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core ML ───────────────────────────────────────────────────────
torch>=2.1.0                         # PyTorch (install separately with CUDA)
transformers>=4.40.0                 # HuggingFace Transformers
datasets>=2.20.0                     # HuggingFace Datasets
tokenizers>=0.19.0

# ── Fine-tuning ───────────────────────────────────────────────────
trl>=0.9.0                           # TRL SFTTrainer
peft>=0.11.0                         # LoRA / QLoRA
bitsandbytes>=0.43.0                 # 4-bit quantization
accelerate>=0.30.0                   # Multi-GPU / distributed training

# ── Utilities ─────────────────────────────────────────────────────
numpy>=1.24.0
scikit-learn>=1.3.0                  # Metrics
python-dotenv==1.0.1
tqdm>=4.66.0

# ── Optional: Monitoring ──────────────────────────────────────────
# wandb                              # Weights & Biases experiment tracking
# tensorboard                        # TensorBoard logging

# ── PyTorch install (pick your CUDA version):
# CUDA 12.1:  pip install torch --index-url https://download.pytorch.org/whl/cu121
# CUDA 11.8:  pip install torch --index-url https://download.pytorch.org/whl/cu118
# CPU only:   pip install torch --index-url https://download.pytorch.org/whl/cpu
